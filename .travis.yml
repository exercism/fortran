dist: trusty
sudo: false 
language: fortran

install:
  - sudo apt-get -y install gfortran
  - sudo apt-get -y install cmake
script:
  - bin/fetch-configlet
  - bin/configlet lint .
  - cmake -G "Unix Makefiles"
  - make

